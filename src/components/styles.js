// TODO: make dynamic breakpoints to adjust for different screen sizes
// TODO: add styling (color, font size, box shadow) so data is easier to navigate
const styles = () => (
  {
    tableRoot: {
      '& > *': {
        borderBottom: 'unset',
      },
    },
    keyTable: {

    },
    categoryTable: {

    }
  }
);

export default styles;